{
  "name": "@midnight-ntwrk/vault-contract",
  "version": "1.0.0",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "compile": "compact compile src/vault.compact src/managed/vault",
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@midnight-ntwrk/compact-js": "^2.5.0"
  }
}
